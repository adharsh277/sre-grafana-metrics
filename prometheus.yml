global:
  scrape_interval: 15s   # How often to scrape targets

scrape_configs:
  - job_name: 'app-metrics'
    static_configs:
      - targets: ['localhost:9090']   # Replace later with your app/metrics endpoint
